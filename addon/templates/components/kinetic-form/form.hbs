<h1>{{title}}</h1>

{{yield}}

{{#unless readOnly}}
  <fieldset>
    <button type="submit" disabled={{if (or isInvalid hasPendingChanges) true}}>
      {{if hasPendingChanges 'Auto-saving...' 'Submit'}}
    </button>
  </fieldset>
{{/unless}}
