<h2 id={{this.anchor}}>{{@field.title}}</h2>

{{#each @field.items as |item|}}
  {{component 
    item.componentName 
    field=item 
    error=(get this.changeset.error item.key) 
    value=(get this.changeset item.key) 
    update=(action this.update item.key)
  }}
{{/each}}
