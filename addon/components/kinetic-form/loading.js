import Component from '@ember/component';
import layout from '../../templates/components/kinetic-form/loading';

export default Component.extend({
  layout,
  classNames: ['kinetic-form--loading']
});
